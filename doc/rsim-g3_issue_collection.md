- [x] link generator issue while the fileset is a multiple level path like:
	`verilog 'logical/etc/hdl/gem_gxl.v'`, then only `hdl/gem_gxl.v` will be used to link with current node location.
	this is high priority issue and need to be fixed.
