# Building
## feature based building flow
A certain feature shall be configured to build different HDL or DV files, the feature file is of .yml type, and by which will generate different settings at the out/common dir, with `*-features.*` file format, so that different generators can read these feature files and build required HDL or DV files.

