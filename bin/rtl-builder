#! /usr/bin/env ruby
"""
Tool to build verilog module according to the erb formatted base and component datafile generated by rsim.
options required:
1. source erb file.
2. component datafile
2.1. get the vlnv info, filter out the 'n' as module name.
2.2. get the ports or bus information, if has bus info, then need get the bus datafile
3. target path to be generated the verilog module.
4. support extra specify the module name.
"""


## main, description
def main ##{{{
	#TODO, major steps:
	# command option process, 
	# - the component datafile, feature file.
	# - the source erb file.
	# - the target path to generate the verilog module.
	# parse the component datafile into ErbProcessor
	# - gets the vlnv
	# - gets the ports and bus information, bus information need get the ports
	# parse the feature file into ErbProcessor
	# build the verilog module by ErbProcessor with defined features, and ports
end ##}}}

